{% extends 'TGUdecBundle::layout.html.twig' %}

{% block contenido -%}
    <header><h3>Asesores</h3></header>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Director</th>
                <th>Jurado</th>
                <th>Asesmetd</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('asesores_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.director }}</td>
                <td>{{ entity.jurado }}</td>
                <td>{{ entity.asesmetd }}</td>
                <td>{{ entity.estado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('asesores_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('asesores_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('asesores_new') }}">
                Crear nuevo Asesor
            </a>
        </li>
    </ul>
    {% endblock %}
