{% extends 'TGUdecBundle::layout.html.twig' %}

{% block contenido -%}
    <header><h3>Archivos de Trabajo de Grado</h3></header>

    <table table id="example" name="example"  class="display" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adjuntos</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('archivostg_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><ul>
                    <li>Resumen <a href="{{ path("tg_udec_homepage") }}adjuntos/resume/{{ entity.resumen }}">{{ entity.resumen }}</a></li>
                    <!-- {#<li><a href="{{ path("tg_udec_homepage") }}adjuntos/{{ entity.abstrc }}">{{ entity.abstrc }}</a></li>#} -->
                    <li>Articulo<a href="{{ path("tg_udec_homepage") }}adjuntos/articulo/{{ entity.articulo }}">{{ entity.articulo }}</a></li>
                    <li>Documento <a href="{{ path("tg_udec_homepage") }}adjuntos/documento/{{ entity.doc }}">{{ entity.doc }}</a></li>
                    <li>Manual tecnico <a href="{{ path("tg_udec_homepage") }}adjuntos/manualTecn/{{ entity.manualTecn }}">{{ entity.manualTecn }}</a></li>
                    <li>Manual de usuario <a href="{{ path("tg_udec_homepage") }}adjuntos/manualUsr/{{ entity.manualUsr }}">{{ entity.manualUsr }}</a></li>
                    {#<li><a href="{{ path("tg_udec_homepage") }}adjuntos/{{ entity.codigoSw }}">{{ entity.codigoSw }}</a></li>#}
                    {#<li>Software <a href="{{ path("tg_udec_homepage") }}adjuntos/software/{{ entity.software }}">{{ entity.software }}</a></li>#}
                    </ul></td>
                <td>
                <ul><!--
                    <li>
                        <a href="{{ path('archivostg_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('archivostg_edit', { 'id': entity.id }) }}">edit</a>
                    </li>-->
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('archivostg_new') }}">
                Crear nuevo Archivo de Trabajo de Grado
            </a>
        </li>
    </ul>
    {% endblock %}
